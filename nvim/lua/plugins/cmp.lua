return {
  "saghen/blink.cmp",
  opts = {
    completion = {
      list = {
        selection = {
          preselect = false, -- Don't auto-select first item, Enter = new line
        },
      },
    },
  },
}
